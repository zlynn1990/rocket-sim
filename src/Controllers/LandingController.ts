import { IController } from "./IController";

export class LandingController implements IController {
  update(time: number, dt: number): void {
    throw new Error("Method not implemented.");
  }
}
